const initialState = {
    
    tasks : [],
}

const reducer = (state = initialState, action) => {

    

    switch(action.type){
        case 'ADD_NOTE' : 
        return 
    }

    return state;
}
console.log("🚀 ~ reducer ~ action:", action)

export default reducer;